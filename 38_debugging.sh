#!/bin/bash

set -x     #this keyword used to test and debug the script, as it step by step displays the flow of script

set -e     #this keyword stops the script whenever command fails

pwd
whoami
date
cd /root
hostname
